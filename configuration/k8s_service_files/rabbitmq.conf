## DEFAULT SETTINGS ARE NOT MEANT TO BE TAKEN STRAIGHT INTO PRODUCTION
## see https://www.rabbitmq.com/configure.html for further information
## on configuring RabbitMQ

## allow access to the guest user from anywhere on the network
## https://www.rabbitmq.com/access-control.html#loopback-users
## https://www.rabbitmq.com/production-checklist.html#users
loopback_users.guest = false

## Send all logs to stdout/TTY. Necessary to see logs when running via
## a container
log.console = true
management.load_definitions = /opt/definitions.json

## RabbitMQ Streams clients receive the broker's advertised endpoint after
## connecting. Use the service DNS name so sidecars in other namespaces can
## resolve the stream broker instead of the RabbitMQ pod hostname.
stream.listeners.tcp.default = 5552
stream.advertised_host = rabbitmq-clusterip.core.svc.cluster.local
stream.advertised_port = 5552
